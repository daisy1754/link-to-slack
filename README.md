### link-to-slack

Simple Android app that receives share intent and send to slack.

#### Setup

> cp .env.example .env

You will need to crate slack app. Check slack-manifest.example.yml

#### Build

./gradlew assembleDebug
./gradlew installDebug